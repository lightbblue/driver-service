import { Proximamente } from '@/components/ui/proximamente';

export default function PerfilScreen() {
  return (
    <Proximamente
      titulo="Perfil"
      requisito="RF-VIEW-005 · MUST"
      detalle="Pantalla comprometida en la Entrega 1. En construcción."
    />
  );
}

import { Proximamente } from '@/components/ui/proximamente';

/** Pantalla SHOULD de la Entrega 1: fuera del alcance comprometido. */
export default function MapaScreen() {
  return (
    <Proximamente
      titulo="Mecánicos cerca tuyo"
      requisito="RF-VIEW-007 · SHOULD"
      detalle="El mapa con los mecánicos disponibles se implementa si queda capacidad en el sprint, después de las cinco pantallas MUST."
    />
  );
}
